version: '3'
services:
    node:
        build: .
        volumes:
            - .:/usr/src/app
        ports:
            - "8080:8080"
        command: node_modules/.bin/nodemon --exec npm run watch -- /usr/src/app/server.js
        environment:
            APP_PORT: 8080
            APP_ENV: dev
